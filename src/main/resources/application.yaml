spring:
  application:
    name: employee-service
  datasource:
    url: jdbc:mysql://localhost:3306/backend?useSSL=false&&createDatabaseIfNotExist=true
    username: root
    password: mysql@237
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update

server:
  port: 8000

logging:
  level:
    org:
      springframework:
        security=DEBUG: